name: Deploy to Server

on:
  push:
    branches:
      - main  # 推送到main分支时触发
  workflow_dispatch:  # 支持手动触发

jobs:
  deploy:
    name: Deploy Application
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代码
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      # 2. 设置Node.js环境
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: |
            frontend/package-lock.json
            backend/package-lock.json

      # 3. 安装前端依赖
      - name: 📦 Install Frontend Dependencies
        working-directory: ./frontend
        run: npm ci

      # 4. 构建前端
      - name: 🏗️ Build Frontend
        working-directory: ./frontend
        run: npm run build

      # 5. 部署到服务器
      - name: 🚀 Deploy to Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          debug: true
          script: |
            # 添加 git 安全目录
            git config --global --add safe.directory /var/www/sld-diy

            # 进入项目目录
            cd /var/www/sld-diy

            # 拉取最新代码
            git pull origin main

            # 构建前端（在服务器上构建）
            cd frontend
            npm install
            npm run build
            cd ..

            # 重启Docker服务（只重启backend，利用volume自动更新代码）
            sudo docker-compose restart backend

            # 等待服务启动
            sleep 20

            # 运行数据库迁移（如果有schema变更）
            sudo docker-compose exec -T backend npx prisma migrate deploy || echo "No migrations to run"

            # 检查服务状态
            sudo docker-compose ps

            # 显示backend日志（最后20行）
            sudo docker-compose logs --tail=20 backend

      # 6. 健康检查
      - name: 🏥 Health Check
        run: |
          sleep 30
          echo "检查前端服务..."
          curl -f http://${{ secrets.SERVER_HOST }}:9000/ || echo "⚠️ Frontend check failed"
          echo "检查后端服务..."
          curl -f http://${{ secrets.SERVER_HOST }}:3001/api/v1/health || echo "⚠️ Backend check failed"

      # 7. 通知部署结果
      - name: 📢 Deployment Notification
        if: always()
        run: |
          if [ ${{ job.status }} == 'success' ]; then
            echo "✅ 部署成功！"
            echo "🌐 前端访问地址: http://${{ secrets.SERVER_HOST }}:9000"
            echo "🔧 后端API地址: http://${{ secrets.SERVER_HOST }}:3001"
          else
            echo "❌ 部署失败！请检查日志"
          fi
